---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: ironcoremetalclustertemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: IroncoreMetalClusterTemplate
    listKind: IroncoreMetalClusterTemplateList
    plural: ironcoremetalclustertemplates
    shortNames:
    - imct
    singular: ironcoremetalclustertemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IroncoreMetalClusterTemplate is the Schema for the ironcoremetalclustertemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec is the desired state of the IroncoreMetalClusterTemplate.
            properties:
              template:
                description: IroncoreMetalClusterTemplateResource describes the data
                  needed to create a IroncoreMetalCluster from a template.
                properties:
                  metadata:
                    description: |-
                      Standard object's metadata.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
                    minProperties: 1
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: IroncoreMetalClusterSpec defines the desired state
                      of IroncoreMetalCluster
                    properties:
                      clusterNetwork:
                        description: Cluster network configuration.
                        minProperties: 1
                        properties:
                          apiServerPort:
                            description: |-
                              apiServerPort specifies the port the API Server should bind to.
                              Defaults to 6443.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          pods:
                            description: pods is the network ranges from which Pod
                              networks are allocated.
                            properties:
                              cidrBlocks:
                                description: cidrBlocks is a list of CIDR blocks.
                                items:
                                  maxLength: 43
                                  minLength: 1
                                  type: string
                                maxItems: 100
                                minItems: 1
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - cidrBlocks
                            type: object
                          serviceDomain:
                            description: serviceDomain is the domain name for services.
                            maxLength: 253
                            minLength: 1
                            type: string
                          services:
                            description: services is the network ranges from which
                              service VIPs are allocated.
                            properties:
                              cidrBlocks:
                                description: cidrBlocks is a list of CIDR blocks.
                                items:
                                  maxLength: 43
                                  minLength: 1
                                  type: string
                                maxItems: 100
                                minItems: 1
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - cidrBlocks
                            type: object
                        type: object
                      controlPlaneEndpoint:
                        description: ControlPlaneEndpoint represents the endpoint
                          used to communicate with the control plane.
                        minProperties: 1
                        properties:
                          host:
                            description: host is the hostname on which the API server
                              is serving.
                            maxLength: 512
                            minLength: 1
                            type: string
                          port:
                            description: port is the port on which the API server
                              is serving.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                        type: object
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
